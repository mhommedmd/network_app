# 🔍 اختبار نظام المعاملات

## خطوات الاختبار

### 1. تأكد من Firebase Console

افتح: https://console.firebase.google.com/project/fir-networkapp/firestore/data/transactions

**تحقق من:**
- ✅ هل توجد مستندات في مجموعة `transactions`؟
- ✅ ما هي الحقول الموجودة في كل مستند؟
- ✅ ما قيم `vendorId` و `networkId`؟

### 2. راقب Debug Console

عند فتح تبويب المعاملات، يجب أن ترى:

```
🔍 Setting up transactions stream: vendorId=XXX, networkId=YYY
```

**سجل هذه القيم!** ثم قارنها مع البيانات في Firebase Console.

### 3. تحقق من الفهارس

افتح: https://console.firebase.google.com/project/fir-networkapp/firestore/indexes

**تأكد من أن جميع الفهارس:**
- ✅ Status: Enabled (أخضر)
- ❌ ليس Building أو Error

### 4. اختبار كامل

**أ. أرسل طلب جديد:**
1. سجل دخول كمتجر
2. أرسل طلب كروت جديد
3. سجّل orderId من Debug Console

**ب. وافق على الطلب:**
1. سجل دخول كصاحب الشبكة
2. وافق على الطلب
3. راقب Debug Console - يجب أن ترى:
   ```
   ✅ Transaction recorded for order: order_XXX
      - vendorId: YYY
      - networkId: ZZZ
   ```

**ج. افتح تبويب المعاملات:**
1. سجل دخول كمتجر
2. افتح تفاصيل الشبكة
3. اذهب لتبويب "المعاملات"
4. راقب Debug Console

---

## ما يجب أن تراه

### في Debug Console:
```
🔍 Setting up transactions stream: vendorId=YYY, networkId=ZZZ
📥 Transactions snapshot received: 1 transactions
   - Transaction: trans_001, type: charge, amount: 450.0
✅ Parsed 1 transactions
📋 Transactions loaded for network: 1
```

### في الواجهة:
```
┌─────────────────────────────────┐
│ الرصيد الحالي: 450 ر.ي         │
├─────────────────────────────────┤
│ ⬆️ مدين              [مكتمل]   │
│ طلب كروت - 2 باقة - 5 كرت    │
│ منذ دقيقة                       │
│                     450 ر.ي    │
└─────────────────────────────────┘
```

---

## إذا لم تظهر المعاملات

### تحقق من:

1. **vendorId و networkId مطابقان؟**
   ```
   في المعاملة: vendorId=ABC, networkId=XYZ
   في الاستعلام: vendorId=ABC, networkId=XYZ
   يجب أن يكونا متطابقين تماماً!
   ```

2. **حقل `date` موجود؟**
   ```
   المعاملة يجب أن تحتوي على:
   - date: Timestamp
   ```

3. **الفهرس جاهز؟**
   ```
   transactions:
     - vendorId (ASC) + networkId (ASC) + date (DESC)
   Status: Enabled ✅
   ```

4. **Firebase Rules صحيحة؟**
   ```
   match /transactions/{transactionId} {
     allow read: if isAuthenticated();
   }
   ```

---

## أرسل لي

إذا استمرت المشكلة، أرسل:

1. **vendorId و networkId** من Debug Console
2. **عدد المعاملات** في Firebase Console
3. **أي رسائل خطأ** في Debug Console
4. **حالة الفهارس** (Enabled/Building/Error)

