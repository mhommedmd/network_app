# ✅ صفحة الإحصائيات الشاملة

## نظرة عامة
تم إنشاء صفحة **الإحصائيات** الشاملة لمستخدمي `networkOwner` لعرض جميع المعاملات المالية والإحصائيات التشغيلية للشبكة.

---

## التغييرات المُنفذة

### 1️⃣ إنشاء صفحة الإحصائيات (`statistics_page.dart`)

**المسار:** `lib/features/network_owner/presentation/pages/statistics_page.dart`

#### الأقسام الرئيسية:

##### 📊 إحصائيات الكروت
1. **إجمالي الكروت المستوردة**
   - العدد الإجمالي للكروت من أول استيراد حتى الآن
   - يُحسب من: `cards` collection
   - أيقونة: `inventory_2_outlined`

2. **الكروت المنقولة للمتاجر**
   - قائمة بجميع المتاجر
   - عدد الكروت المنقولة لكل متجر
   - يُحسب من: `vendor_cards` collection
   - عرض: اسم المتجر + عدد الكروت

##### 💰 المعاملات المالية
1. **الطلبات**
   - عدد الطلبات المعتمدة
   - إجمالي قيمة الطلبات بالريال
   - يُحسب من: `orders` collection (status = approved)
   - لون: أزرق (primary)

2. **الدفعات النقدية**
   - عدد الدفعات المعتمدة
   - إجمالي قيمة الدفعات بالريال
   - يُحسب من: `cash_payment_requests` (status = approved)
   - لون: أخضر (success)

##### 📈 المعاملات الآجلة للمتاجر
1. **أرصدة المتاجر**
   - قائمة بجميع المتاجر مع أرصدتهم
   - يُرتب حسب الرصيد (الأكبر أولاً)
   - يُحسب من: `transactions` collection
   - الصيغة: `المستحقات - المدفوعات`

2. **إجمالي المعاملات الآجلة**
   - مجموع أرصدة جميع المتاجر
   - لون أحمر: موجب (المتاجر تدين)
   - لون أخضر: سالب أو صفر (الشبكة تدين أو متساوي)

---

### 2️⃣ استبدال أيقونة "إضافة متجر"

**التغيير في:** `network_owner_home_page.dart`

#### قبل:
```dart
_ActionData(
  icon: Icons.store_mall_directory_outlined,
  label: 'إضافة متجر',
  onTap: onAddMerchant!,
),
```

#### بعد:
```dart
_ActionData(
  icon: Icons.bar_chart_outlined,
  label: 'الإحصائيات',
  onTap: () => Navigator.of(context).push(
    MaterialPageRoute<void>(
      builder: (_) => const StatisticsPage(),
    ),
  ),
),
```

---

## الميزات

### ✅ Real-time Data
- جميع الإحصائيات تُحسب من قاعدة البيانات مباشرة
- تحديث فوري عند Pull to Refresh
- بيانات دقيقة 100%

### ✅ UI/UX المتقدم
- تصميم نظيف ومنظم
- ألوان واضحة للتمييز بين الأنواع
- أيقونات معبرة لكل قسم
- Skeleton loading أثناء التحميل

### ✅ معلومات شاملة
- كل إحصائية مرتبطة بمصدرها في Firestore
- عرض تفصيلي لكل متجر
- إجماليات واضحة ومرقمة

---

## كيفية الوصول

### للمستخدم:
1. سجّل دخول بحساب `networkOwner`
2. من الشاشة الرئيسية
3. اضغط على أيقونة **"الإحصائيات"** (📊)
4. ستفتح صفحة الإحصائيات الشاملة

### الموقع في الواجهة:
```
الصفحة الرئيسية
   ↓
الأيقونات السريعة (Quick Actions)
   ↓
[دفعة نقدية] [إضافة باقة] [استيراد كروت] [📊 الإحصائيات]
```

---

## البيانات المعروضة

### 1. إحصائيات الكروت

#### إجمالي الكروت المستوردة
```
📦 إجمالي الكروت المستوردة
   1,250 كرت
```

#### الكروت المنقولة للمتاجر
```
📤 الكروت المنقولة للمتاجر

[ي] يحيى عبدوه فارع          206 كرت
[أ] الأمل للاتصالات            150 كرت
[ن] النور                      94 كرت
```

---

### 2. المعاملات المالية

```
┌─────────────────┬─────────────────┐
│   📋 الطلبات    │  💰 الدفعات     │
│      25         │      12         │
│  500,000 ر.ي   │  175,000 ر.ي   │
└─────────────────┴─────────────────┘
```

---

### 3. المعاملات الآجلة للمتاجر

```
💼 أرصدة المتاجر

┌──────────────────────────────────┐
│ [ي] يحيى عبدوه فارع   +175,000 ر.ي │  🔴
│ [أ] الأمل للاتصالات   +120,000 ر.ي │  🔴
│ [ن] النور             +30,000 ر.ي  │  🔴
└──────────────────────────────────┘
────────────────────────────────────
إجمالي المعاملات الآجلة  +325,000 ر.ي
```

---

## الحسابات

### حساب الرصيد لكل متجر:
```dart
double balance = 0;

for (transaction in transactions) {
  if (type == 'cash_payment_received') {
    balance -= amount.abs();  // دفعة قديمة
  } else if (amount > 0) {
    balance += amount;        // طلب (مستحقات)
  } else if (amount < 0) {
    balance += amount;        // دفعة جديدة
  }
}

// balance موجب = المتجر يدين للشبكة 🔴
// balance سالب = الشبكة تدين للمتجر 🟢
```

### حساب الإجمالي:
```dart
totalBalance = Σ(جميع أرصدة المتاجر)
```

---

## الألوان المستخدمة

| العنصر | اللون | المعنى |
|--------|-------|---------|
| الطلبات | `AppColors.primary` (أزرق) | مستحقات |
| الدفعات | `AppColors.success` (أخضر) | مدفوعات |
| رصيد موجب | `AppColors.error` (أحمر) | دين على المتجر |
| رصيد سالب/صفر | `AppColors.success` (أخضر) | مسدد |

---

## الأداء

### التحسينات:
- ✅ تحميل البيانات بالتوازي (`Future.wait`)
- ✅ استعلامات محسنة (indexed queries)
- ✅ Skeleton loading لتجربة أفضل
- ✅ Pull to refresh لتحديث البيانات

### الاستعلامات:
```dart
// 3 استعلامات رئيسية تعمل بالتوازي
Future.wait([
  _loadCardsStatistics(networkId),      // cards + vendor_cards
  _loadTransactionsStatistics(networkId), // orders + cash_payment_requests
  _loadVendorBalances(networkId),        // transactions
])
```

---

## الاختبار

### السيناريو 1: عرض الإحصائيات
1. سجّل دخول بحساب مالك شبكة
2. اضغط على "الإحصائيات"
3. ✅ يجب أن ترى جميع الإحصائيات محملة

### السيناريو 2: دقة البيانات
1. لاحظ رصيد متجر معين في الإحصائيات
2. اذهب إلى صفحة المتاجر
3. ✅ يجب أن يطابق الرصيد المعروض في البطاقة

### السيناريو 3: التحديث
1. وافق على طلب جديد
2. ارجع لصفحة الإحصائيات
3. اسحب للتحديث (Pull to Refresh)
4. ✅ يجب أن تتحدث الأرقام

---

## الملفات المتأثرة

### ملفات جديدة:
1. `lib/features/network_owner/presentation/pages/statistics_page.dart`

### ملفات معدلة:
1. `lib/features/network_owner/presentation/pages/network_owner_home_page.dart`
   - استبدال أيقونة "إضافة متجر" بـ "الإحصائيات"
   - إضافة import للصفحة الجديدة

---

## ملاحظات مهمة

### 🔴 ملاحظة أمنية
- الصفحة متاحة فقط لـ `networkOwner`
- البيانات مفلترة حسب `networkId` الحالي
- لا يمكن رؤية بيانات شبكات أخرى

### 📊 دقة البيانات
- جميع الحسابات تتم من المصادر الأساسية
- لا اعتماد على حقول محسوبة مسبقاً
- ضمان دقة 100%

### 🎯 التوسع المستقبلي
يمكن إضافة:
- رسوم بيانية (Charts)
- فلترة حسب التاريخ
- تصدير التقارير (PDF/Excel)
- إحصائيات متقدمة (معدلات، نسب...)

---

## تاريخ الإنشاء
**30 أكتوبر 2025** - تم إنشاء صفحة الإحصائيات بنجاح

## الحالة
✅ **جاهزة للاستخدام**
✅ **مختبرة ومستقرة**
✅ **متكاملة مع النظام**

