# 🚀 كيفية استخدام أداة فحص وتصحيح قاعدة البيانات

## 📱 الوصول السريع للأداة

### الطريقة المؤقتة (للاختبار):

1. افتح أي ملف في `lib/features/network_owner/presentation/pages/`
2. أضف هذا الكود في أي `build` method:

```dart
// أضف في أعلى الملف
import 'database_diagnostic_page.dart';

// ثم أضف FloatingActionButton
@override
Widget build(BuildContext context) {
  return Scaffold(
    // ... الكود الموجود
    floatingActionButton: FloatingActionButton(
      onPressed: () {
        Navigator.push(
          context,
          MaterialPageRoute(
            builder: (_) => const DatabaseDiagnosticPage(),
          ),
        );
      },
      child: const Icon(Icons.bug_report),
      tooltip: 'فحص قاعدة البيانات',
    ),
  );
}
```

---

## 🔍 استخدام أداة الفحص

### الخطوة 1: تشغيل الفحص
- عند فتح الصفحة، سيبدأ الفحص تلقائياً
- انتظر حتى ينتهي الفحص (سيستغرق ثوانٍ)

### الخطوة 2: قراءة النتائج

#### 🟢 بطاقة خضراء = ممتاز
```
✅ نتيجة فحص المتاجر
إجمالي: 5
صحيح: 5
به مشاكل: 0
```
**لا حاجة لأي إجراء**

#### 🟡 بطاقة صفراء = تحذير (غير حرج)
```
⚠️ نتيجة فحص الطلبات
يحتوي على vendorStoreName: 3
بدون vendorStoreName: 7
```
**اختياري:** يمكن التصحيح بزر "تصحيح تلقائي"

#### 🔴 بطاقة حمراء = خطأ حرج
```
❌ متجر الأمل - Document ID لا يطابق User ID
```
**مطلوب:** يجب التصحيح (راجع الدليل الكامل)

---

## 🔧 استخدام التصحيح التلقائي

### متى تستخدمه؟
- عندما ترى بطاقات صفراء عن حقول مفقودة
- لإصلاح البيانات القديمة بعد التحديثات

### كيف تستخدمه؟

1. **اضغط على أيقونة "المطرقة" 🔨** في شريط الأدوات
2. **اقرأ الرسالة التوضيحية:**
   ```
   سيتم تصحيح البيانات القديمة تلقائياً:
   ✅ إضافة حقل vendorStoreName للطلبات القديمة
   ✅ إضافة حقل vendorStoreName للمعاملات القديمة
   ...
   ```
3. **اضغط "نعم، صحّح الآن"**
4. **انتظر حتى ينتهي التصحيح**
5. **سترى نتيجة التصحيح:**
   ```
   ✅ اكتمل التصحيح
   📦 الطلبات: 7
   💰 المعاملات: 12
   📊 الإجمالي: 25 سجل
   ```
6. **سيُعاد تشغيل الفحص تلقائياً** للتأكد من النتيجة

---

## 📋 ماذا يفعل التصحيح التلقائي؟

### للطلبات (orders):
```
قبل:
{
  "vendorId": "I1Xk9X7a...",
  "vendorName": "أحمد محمد"
}

بعد:
{
  "vendorId": "I1Xk9X7a...",
  "vendorName": "أحمد محمد",
  "vendorStoreName": "متجر الأمل" ← جديد
}
```

### للمعاملات (transactions):
```
نفس الإجراء - يضيف vendorStoreName
```

### لكروت المتاجر (vendor_cards):
```
نفس الإجراء - يضيف vendorStoreName
```

### لطلبات الدفع (cash_payment_requests):
```
نفس الإجراء - يضيف vendorStoreName
```

---

## 🎯 الأسئلة الشائعة

### س: هل التصحيح التلقائي آمن؟
**ج:** نعم! العملية:
- ✅ لا تحذف أي بيانات
- ✅ تضيف فقط حقول مفقودة
- ✅ تحصل على البيانات من `vendors` collection
- ✅ لا تعدّل أي حقول موجودة

---

### س: ماذا لو فشل التصحيح؟
**ج:** البيانات الأصلية تبقى كما هي، ولن يحدث أي ضرر.

---

### س: هل أحتاج لتشغيل التصحيح لكل البيانات؟
**ج:** لا، فقط للبيانات القديمة:
- البيانات الجديدة (بعد التحديثات) صحيحة تلقائياً
- البيانات القديمة تعمل بدون مشاكل (fallback موجود)
- التصحيح مفيد فقط لعرض أفضل

---

### س: كم مرة أحتاج لتشغيل الفحص؟
**ج:** 
- مرة واحدة بعد التحديث لمعرفة الوضع
- اختياري: كل شهر للمتابعة
- عند ملاحظة أي مشكلة

---

## 📊 نسخ التقرير

اضغط على أيقونة "النسخ" 📋 في شريط الأدوات:
- سيُنسخ تقرير كامل إلى الحافظة
- يمكنك لصقه في ملف نصي
- مفيد للمراجعة أو المشاركة

---

## ⚠️ المشاكل الحرجة (تحتاج تدخل يدوي)

إذا رأيت:
```
❌ متجر الأمل - Document ID لا يطابق User ID
```

**لا يمكن تصحيحها تلقائياً!**

راجع الدليل الكامل: [DATABASE_DIAGNOSTIC_GUIDE.md](./DATABASE_DIAGNOSTIC_GUIDE.md)

---

## 🎉 نصائح للنجاح

1. ✅ شغّل الفحص قبل التصحيح
2. ✅ اقرأ النتائج بعناية
3. ✅ استخدم التصحيح التلقائي للتحذيرات الصفراء
4. ✅ انسخ التقرير قبل وبعد التصحيح
5. ✅ شغّل الفحص مرة أخرى بعد التصحيح للتأكد

---

## 📞 المساعدة

إذا واجهت مشكلة:
1. انسخ التقرير (زر 📋)
2. راجع الدليل الكامل: [DATABASE_DIAGNOSTIC_GUIDE.md](./DATABASE_DIAGNOSTIC_GUIDE.md)
3. تواصل مع الدعم مع نسخة من التقرير

---

**آخر تحديث:** 29 أكتوبر 2025


