# دليل الاختبار السريع - التحقق من الهاتف عبر SMS

## 🚀 الاختبار السريع (5 دقائق)

### الخيار 1: الاختبار في وضع Debug (بدون SMS فعلي)

```bash
# 1. شغّل التطبيق في وضع Debug
flutter run

# 2. في شاشة التسجيل:
- اختر نوع الحساب
- أدخل أي رقم هاتف (مثل: 777123456)
- أدخل كلمة مرور
- اضغط "التالي"

# ✅ سيتم تخطي التحقق من OTP تلقائياً في وضع Debug
```

### الخيار 2: الاختبار باستخدام Test Phone Numbers

```bash
# 1. أضف رقم اختبار في Firebase Console:
Phone: +967777777777
Code: 123456

# 2. شغّل التطبيق:
flutter run --release

# 3. استخدم الرقم +967777777777
# 4. أدخل الكود 123456 في شاشة OTP
```

### الخيار 3: الاختبار الحقيقي (SMS فعلي)

```bash
# ⚠️ يتطلب:
# - مشروع Firebase على Blaze Plan
# - SHA-1 مضاف بشكل صحيح
# - Phone Auth مفعّل

# 1. شغّل التطبيق:
flutter run --release

# 2. استخدم رقم هاتف يمني حقيقي
# 3. انتظر وصول SMS (قد يستغرق 1-2 دقيقة)
# 4. أدخل الكود من SMS
```

## 🧪 سيناريوهات الاختبار

### ✅ السيناريو 1: تسجيل ناجح

```
1. شاشة التسجيل
   - نوع الحساب: مالك شبكة
   - الهاتف: 777123456
   - كلمة المرور: Test123!
   
2. إرسال OTP
   - ✅ يظهر "جاري الإرسال..."
   - ✅ ينتقل لشاشة OTP
   
3. شاشة OTP
   - ✅ يظهر الرقم: +967777123456
   - ✅ 6 حقول لإدخال الكود
   - ✅ مؤقت: "إعادة الإرسال بعد 60 ثانية"
   
4. إدخال الكود الصحيح
   - ✅ رسالة نجاح
   - ✅ ينتقل لإكمال بيانات الحساب
   
5. إكمال التسجيل
   - ✅ إنشاء الحساب بنجاح
   - ✅ ينتقل للشاشة الرئيسية
```

### ❌ السيناريو 2: كود خاطئ

```
1. إدخال كود خاطئ (مثل: 000000)
2. ✅ يظهر خطأ: "رمز التحقق غير صحيح"
3. ✅ يتم مسح الحقول
4. ✅ يمكن المحاولة مرة أخرى
```

### 🔄 السيناريو 3: إعادة إرسال OTP

```
1. انتظر 60 ثانية
2. ✅ يتغير النص إلى "إعادة الإرسال" (باللون الأزرق)
3. اضغط "إعادة الإرسال"
4. ✅ يُرسل كود جديد
5. ✅ يبدأ المؤقت من جديد (60 ثانية)
```

### ↩️ السيناريو 4: تغيير رقم الهاتف

```
1. في شاشة OTP
2. اضغط "تغيير رقم الهاتف"
3. ✅ يعود لشاشة التسجيل
4. ✅ رقم الهاتف موجود (يمكن تعديله)
```

## 🐛 استكشاف الأخطاء

### الخطأ: "فشل إرسال رمز التحقق"

**الأسباب المحتملة:**
```
1. ❌ Phone Auth غير مفعّل في Firebase
   الحل: فعّل من Authentication → Sign-in method

2. ❌ المشروع على Spark Plan
   الحل: ترقية إلى Blaze Plan

3. ❌ SHA-1 غير مضاف
   الحل: أضف SHA-1 من gradlew signingReport

4. ❌ رقم الهاتف غير صحيح
   الحل: تأكد من التنسيق +967XXXXXXXXX
```

### الخطأ: "رمز التحقق غير صحيح"

**الأسباب المحتملة:**
```
1. ❌ الكود منتهي الصلاحية (أكثر من 5 دقائق)
   الحل: اطلب كود جديد

2. ❌ إدخال كود خاطئ
   الحل: تحقق من الكود في SMS

3. ❌ استخدام كود قديم
   الحل: استخدم آخر كود وصل
```

### الخطأ: "يجب التحقق من رقم الهاتف أولاً"

**السبب:**
```
❌ محاولة التسجيل بدون التحقق من OTP في Production mode

الحل:
1. تأكد من المرور بشاشة OTP
2. تحقق من الكود بنجاح
3. ثم أكمل التسجيل
```

## 📊 متابعة الاختبار في Firebase Console

### عرض محاولات التحقق

```
1. افتح Firebase Console
2. اذهب إلى Authentication → Users
3. ستجد المستخدمين الجدد مع:
   - رقم الهاتف
   - البريد الإلكتروني (مولّد تلقائياً)
   - تاريخ الإنشاء

4. اذهب إلى Authentication → Usage
   - عدد رسائل SMS المرسلة
   - تكلفة الاستخدام
```

## 🔍 فحص الـ Logs

### في VS Code / Android Studio

```bash
# تشغيل مع logs مفصّلة
flutter run --verbose

# البحث عن logs متعلقة بـ OTP
# ابحث عن:
# - "🔄 محاولة إرسال OTP"
# - "✅ تم إرسال OTP بنجاح"
# - "❌ فشل إرسال OTP"
# - "📱 التحقق من OTP"
```

### في Firebase Console

```
1. اذهب إلى Authentication → Usage
2. شاهد:
   - عدد المحاولات الناجحة
   - عدد المحاولات الفاشلة
   - الأخطاء الشائعة
```

## 📱 الاختبار على أجهزة مختلفة

### Android Emulator
```bash
# ⚠️ محدود: لا يمكن استقبال SMS حقيقي
# ✅ يعمل: Test Phone Numbers
# ✅ يعمل: Debug mode (بدون OTP)

flutter run
```

### Android Device (Real)
```bash
# ✅ يعمل بشكل كامل
# ✅ استقبال SMS حقيقي
# ✅ جميع السيناريوهات

flutter run --release
```

### iOS Simulator
```bash
# ⚠️ محدود: لا يمكن استقبال SMS حقيقي
# ✅ يعمل: Test Phone Numbers
# ✅ يعمل: Debug mode (بدون OTP)

flutter run
```

### iOS Device (Real)
```bash
# ✅ يعمل بشكل كامل
# ⚠️ يتطلب: Apple Developer Account
# ⚠️ يتطلب: APN configuration

flutter run --release
```

## ⏱️ الأوقات المتوقعة

```
إرسال OTP:        1-5 ثواني
وصول SMS:         10-120 ثانية (يعتمد على الشبكة)
التحقق من OTP:    1-2 ثواني
إنشاء الحساب:     2-5 ثواني
الإجمالي:         ~15-135 ثانية
```

## 💡 نصائح للاختبار الفعال

### 1. استخدم Debug Mode للتطوير السريع
```dart
// في auth_provider.dart
if (kDebugMode) {
  return bypassRegistrationOtpForTesting(phone);
}
```

### 2. أنشئ Test Phone Numbers للاختبار المتكرر
```
Firebase Console → Authentication → Phone numbers for testing
+967777777777 → 123456
+967777777778 → 654321
```

### 3. راقب الاستخدام والتكلفة
```
Firebase Console → Usage and billing
- ضع حد أقصى للميزانية
- فعّل تنبيهات البريد
```

### 4. احفظ الأرقام المستخدمة للاختبار
```
تجنب:
- استخدام نفس الرقم بشكل متكرر
- تجاوز حدود Firebase (5 SMS/hour per number)

الحل:
- استخدم أرقام مختلفة
- أو انتظر ساعة بين الاختبارات
```

## 📋 Checklist قبل الاختبار

- [ ] Firebase Console: Phone Auth مفعّل
- [ ] SHA-1 مضاف (للـ Android)
- [ ] المشروع على Blaze Plan (للـ Production)
- [ ] Test Phone Numbers مضافة (للاختبار)
- [ ] google-services.json محدّث
- [ ] التطبيق مُعاد بناؤه بعد التغييرات

## 🎯 نتائج متوقعة

### ✅ النجاح يعني:
- رسالة SMS تصل للهاتف
- الكود يعمل بنجاح
- الحساب يُنشأ في Firebase
- المستخدم ينتقل للشاشة الرئيسية

### ❌ الفشل يعني:
- خطأ واضح يظهر للمستخدم
- لا يتم إنشاء حساب جزئي
- يمكن المحاولة مرة أخرى

---

**آخر تحديث:** 2 نوفمبر 2025
**النسخة:** 1.0.0

