# تحديث عرض معلومات الموقع (المحافظة، المديرية، المدينة)

## التحديثات المطبقة

### 1. عملية التسجيل (`register_page.dart`)

تم تحديث عملية التسجيل لحفظ وتحديث معلومات الموقع بشكل كامل:

```dart
// حفظ البيانات في Firestore
final district = _districtController.text.trim();
final city = _cityController.text.trim();

final profileData = <String, dynamic>{
  'governorate': _selectedGovernorate,  // المحافظة
  'district': district,                  // المديرية
  'city': city,                          // المدينة
  // ... باقي البيانات
};

// تحديث User object في الذاكرة
await authProvider.updateUserProfile(
  governorate: _selectedGovernorate,
  district: district,
  address: city, // city يتم حفظه في address
);
```

**ملاحظة**: تم حفظ `city` في حقل `address` في User model للتوافق مع الهيكل الحالي.

### 2. بطاقة عرض المتجر

#### قبل التحديث:
```
📞 رقم الهاتف  |  📍 المحافظة، المديرية
```

#### بعد التحديث:
```
📞 رقم الهاتف
📍 المحافظة - المديرية - المدينة
```

تم فصل الهاتف والموقع في سطرين منفصلين لوضوح أفضل.

### 3. الملفات المُحدثة

#### `register_page.dart`
- ✅ حفظ `governorate`, `district`, `city` في Firestore
- ✅ تحديث User object بعد التسجيل
- ✅ تطبيق التحديث لكل من networkOwner و posVendor

#### `accounts_page.dart`
- ✅ تحديث عرض بطاقة المتجر لإظهار الموقع الكامل
- ✅ عرض: المحافظة - المديرية - المدينة

#### `network_owner_home_page.dart`
- ✅ تحديث عرض المتاجر الأكثر نشاطاً
- ✅ عرض الموقع الكامل: المحافظة - المديرية - المدينة

### 4. صيغة عرض الموقع

تم استخدام صيغة ديناميكية لعرض الموقع:

```dart
'${vendor.governorate}${vendor.district.isNotEmpty ? ' - ${vendor.district}' : ''}${vendor.address.isNotEmpty ? ' - ${vendor.address}' : ''}'
```

**مميزات هذه الصيغة:**
- ✅ تعرض المحافظة دائماً
- ✅ تضيف المديرية فقط إذا كانت موجودة
- ✅ تضيف المدينة فقط إذا كانت موجودة
- ✅ تستخدم الفاصلة " - " للفصل بين الأجزاء

**أمثلة:**
- إذا كانت جميع الحقول موجودة: `صنعاء - التحرير - شارع الزبيري`
- إذا كانت المديرية فقط فارغة: `صنعاء - شارع الزبيري`
- إذا كانت المدينة فقط فارغة: `صنعاء - التحرير`
- إذا كانت المحافظة فقط: `صنعاء`

### 5. عرض الموقع في مختلف الأماكن

#### أ. صفحة الحسابات (accounts_page)
```
┌─────────────────────────────────────┐
│  [أ]  اسم المتجر                    │
│       👤 اسم المالك                 │
│       📞 771234567                   │
│       📍 صنعاء - التحرير - المدينة  │
└─────────────────────────────────────┘
```

#### ب. المتاجر الأكثر نشاطاً (network_owner_home_page)
```
┌─────────────────────────────────────┐
│  [أ]  اسم المتجر                    │
│       👤 اسم المالك                 │
│       📞 771234567                   │
│       📍 صنعاء - التحرير - المدينة  │
└─────────────────────────────────────┘
```

#### ج. صفحة Profile (profile_page)
تعرض الحقول بشكل منفصل:
- **المحافظة**: `governorate`
- **المديرية**: `district`
- **المنطقة/العنوان**: `address` (يحتوي على city)

### 6. البيانات في Firestore

#### لـ posVendor:
```json
{
  "name": "متجر الأمل",
  "ownerName": "أحمد علي",
  "governorate": "صنعاء",
  "district": "التحرير",
  "city": "شارع الزبيري",
  "address": "شارع الزبيري" // نفس city
}
```

#### لـ networkOwner:
```json
{
  "name": "أحمد علي",
  "networkName": "شبكة افاق نت",
  "governorate": "صنعاء",
  "district": "التحرير",
  "city": "المدينة",
  "address": "المدينة" // نفس city
}
```

## التحسينات المستقبلية

يمكن إضافة عرض الموقع في:
- ✅ نتائج البحث عن المتاجر
- ✅ تفاصيل الطلبات
- ✅ قوائم المعاملات
- ✅ أي مكان آخر يعرض معلومات المتجر

## ملاحظات مهمة

1. **التوافق الخلفي**: 
   - البيانات القديمة (بدون city) ستعمل بشكل صحيح
   - الصيغة الديناميكية تتعامل مع الحقول الفارغة

2. **User Model**:
   - `governorate`: المحافظة
   - `district`: المديرية
   - `address`: يحتوي على المدينة/العنوان التفصيلي

3. **VendorModel**:
   - يستخدم نفس الحقول من User model
   - `location` getter لا يزال يعمل (المحافظة + المديرية)

## الاختبار

للتحقق من التحديثات:

1. **إنشاء حساب جديد**:
   - سجل حساب posVendor أو networkOwner
   - تأكد من حفظ المحافظة والمديرية والمدينة

2. **عرض البيانات**:
   - افتح صفحة الحسابات
   - تحقق من عرض الموقع الكامل في بطاقة المتجر

3. **Profile**:
   - افتح الملف الشخصي
   - تحقق من عرض جميع الحقول بشكل صحيح

