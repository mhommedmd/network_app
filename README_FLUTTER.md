# تطبيق إدارة كروت الإنترنت - MikroTik (Flutter)

## 🎉 حالة المشروع
**✅ المشروع جاهز للتطوير** - تم تنظيف جميع ملفات React/TypeScript والاحتفاظ بملفات Flutter فقط.

هذا تطبيق إدارة كروت الإنترنت لشبكات MikroTik مبني بالكامل باستخدام Flutter/Dart.

## 📱 نظرة عامة

تطبيق إدارة كروت الإنترنت لشبكات MikroTik يربط بين مالكي الشبكات وبائعي نقاط البيع الذين يبيعون كروت الإنترنت المدفوعة مسبقاً للمستخدمين النهائيين.

### ✨ المميزات الرئيسية

- 🌐 دعم كامل للغتين العربية والإنجليزية مع RTL
- 👥 نوعان من المستخدمين: مالكو الشبكات وبائعو نقاط البيع
- 🇾🇪 مخصص للاستخدام في اليمن (أرقام هواتف يمنية، ريال يمني)
- 🎨 نظام تصميم موحد باللون الأزرق #2563eb
- 📱 تصميم متجاوب لجميع أحجام الشاشات
- 🔐 نظام مصادقة آمن
- 💳 إدارة شاملة للباقات والكروت

## 🏗 هيكل المشروع

```
lib/
├── main.dart                    # نقطة دخول التطبيق
├── core/                        # الملفات الأساسية
│   ├── theme/                   # نظام الألوان والتصميم
│   │   ├── app_theme.dart
│   │   └── app_colors.dart
│   ├── providers/               # إدارة الحالة
│   │   ├── auth_provider.dart
│   │   └── language_provider.dart
│   ├── router/                  # التنقل
│   │   └── app_router.dart
│   └── localization/           # الترجمة
│       └── app_localizations.dart
├── features/                    # الميزات الرئيسية
│   ├── auth/                    # المصادقة
│   ├── home/                    # الصفحة الرئيسية
│   ├── network_owner/           # صفحات مالك الشبكة
│   ├── pos_vendor/             # صفحات بائع نقطة البيع
│   └── common/                 # الصفحات المشتركة
├── shared/                     # المكونات المشتركة
│   └── widgets/
│       ├── app_button.dart
│       └── app_card.dart
└── assets/                     # الموارد
    └── translations/           # ملفات الترجمة
        ├── ar.json
        └── en.json
```

## 🎨 نظام التصميم

### الألوان الأساسية
```dart
// الألوان الرئيسية
static const Color primary = Color(0xFF2563EB);      // blue-600
static const Color primaryLight = Color(0xFF3B82F6); // blue-500
static const Color primaryDark = Color(0xFF1D4ED8);  // blue-700

// ألوان الحالة
static const Color success = Color(0xFF059669);      // emerald-600
static const Color warning = Color(0xFFD97706);      // amber-600
static const Color error = Color(0xFFDC2626);        // red-600

// التدرجات
static const LinearGradient primaryGradient = LinearGradient(
  colors: [blue600, blue700],
);
```

### المكونات المخصصة

#### AppButton
```dart
AppButton(
  text: 'ابدأ البيع',
  variant: AppButtonVariant.primary,
  size: AppButtonSize.large,
  onPressed: () {},
)
```

#### AppCard
```dart
AppCard(
  variant: AppCardVariant.elevated,
  child: Text('محتوى البطاقة'),
)
```

## 🚀 البدء

### المتطلبات
- Flutter SDK (3.10.0 أو أحدث)
- Dart SDK (3.0.0 أو أحدث)
- Android Studio أو VS Code
- جهاز Android/iOS أو محاكي

### التثبيت
```bash
# نسخ المشروع
git clone [repository-url]
cd mikrotik_cards_manager

# تثبيت المكتبات
flutter pub get

# تشغيل التطبيق
flutter run
```

### إعداد البيئة

1. **Android**: تأكد من إعداد Android SDK
2. **iOS**: تأكد من إعداد Xcode (macOS فقط)
3. **Web**: فعل دعم الويب إذا لزم الأمر

## 📦 المكتبات المستخدمة

### إدارة الحالة
- `provider: ^6.1.1` - إدارة الحالة

### التصميم والواجهة
- `flutter_screenutil: ^5.9.0` - التصميم المتجاوب
- `google_fonts: ^6.1.0` - الخطوط
- `material_design_icons_flutter: ^7.0.7296` - الأيقونات

### التنقل
- `go_router: ^12.1.3` - نظام التنقل

### الشبكة والAPI
- `http: ^1.1.0` - طلبات HTTP
- `dio: ^5.3.2` - عميل HTTP متقدم

### التخزين المحلي
- `shared_preferences: ^2.2.2` - تخزين البيانات محلياً

### المساعدات
- `intl: ^0.18.1` - تنسيق التواريخ والأرقام
- `fluttertoast: ^8.2.4` - رسائل Toast
- `cached_network_image: ^3.3.0` - تخزين الصور مؤقتاً

## 🔧 المميزات المُطبقة

### ✅ تم تطبيقها
- [x] نظام الألوان الموحد
- [x] مكونات التصميم الأساسية (AppButton, AppCard)
- [x] نظام المصادقة الأساسي
- [x] دعم اللغات (العربية/الإنجليزية)
- [x] التنقل بين الصفحات
- [x] الصفحة الرئيسية لبائع نقطة البيع
- [x] صفحة تسجيل الدخول والتسجيل
- [x] صفحة الملف الشخصي

### 🚧 قيد التطوير
- [ ] إدارة الشبكات والباقات
- [ ] عملية البيع الكاملة
- [ ] طلب الكروت
- [ ] نظام المحادثات
- [ ] إدارة المخزون
- [ ] التقارير والإحصائيات
- [ ] ربط قاعدة البيانات

## 📱 واجهات المستخدم

### بائع نقطة البيع
1. **الصفحة الرئيسية**: عرض الإحصائيات والإجراءات السريعة
2. **الشبكات**: عرض الشبكات المتاحة
3. **المحادثات**: التواصل مع مالكي الشبكات
4. **الملف الشخصي**: إدارة الحساب

### مالك الشبكة
1. **الصفحة الرئيسية**: لوحة تحكم بالمبيعات والطلبات
2. **الشبكة**: إدارة الباقات والمخزون
3. **المتاجر**: إدارة بائعي نقاط البيع
4. **المحادثات**: التواصل مع البائعين
5. **الملف الشخصي**: إدارة الحساب

## 🌐 الترجمة

يدعم التطبيق اللغتين العربية والإنجليزية:

```dart
// استخدام الترجمة
final languageProvider = Provider.of<LanguageProvider>(context);
Text(languageProvider.home) // سيعرض "الرئيسية" أو "Home"
```

### إضافة ترجمات جديدة
1. أضف النص في `assets/translations/ar.json`
2. أضف النص في `assets/translations/en.json`
3. أضف الدالة في `LanguageProvider`

## 🔐 المصادقة

النظام يدعم نوعين من المستخدمين:
- `UserType.networkOwner` - مالك شبكة
- `UserType.posVendor` - بائع نقطة بيع

```dart
final authProvider = Provider.of<AuthProvider>(context);
final success = await authProvider.login(
  phone: phoneNumber,
  password: password,
);
```

## 📊 إدارة الحالة

يستخدم التطبيق مكتبة Provider لإدارة الحالة:

```dart
// تسجيل المقدمين
MultiProvider(
  providers: [
    ChangeNotifierProvider(create: (_) => AuthProvider()),
    ChangeNotifierProvider(create: (_) => LanguageProvider()),
  ],
  child: MyApp(),
)
```

## 🎯 التطوير المستقبلي

### المرحلة التالية
1. **ربط قاعدة البيانات**: ربط Supabase أو Firebase
2. **عمليات البيع**: تطبيق نظام البيع الكامل
3. **إدارة المخزون**: تتبع الكروت والباقات
4. **النوتيفيكيشن**: إشعارات push
5. **التقارير**: تقارير المبيعات والإحصائيات

### تحسينات مقترحة
- [ ] وضع مظلم (Dark Mode)
- [ ] أنيميشن أكثر تفاعلاً
- [ ] دعم المزيد من اللغات
- [ ] نظام التخزين المؤقت
- [ ] اختبارات الوحدة والتكامل

## 🤝 المساهمة

1. انشئ فرع جديد للميزة
2. اكتب الكود مع التوثيق
3. اختبر التغييرات
4. أرسل Pull Request

## 📄 الترخيص

هذا المشروع مرخص تحت [MIT License](LICENSE).

---

تم تطوير هذا التطبيق بـ ❤️ باستخدام Flutter