# 🗑️ دليل حذف البيانات من Firebase - تنظيف كامل

## ⚠️ تحذير مهم جداً

**حذف البيانات من Firebase عملية لا يمكن التراجع عنها!**

- ❌ لا يمكن استرجاع البيانات بعد الحذف
- ❌ سيتم حذف جميع الحسابات والمعاملات والبيانات
- ⚠️ تأكد أنك تريد فعلاً حذف كل شيء

---

## 📋 ما سيتم حذفه

### 1. Authentication (حسابات المستخدمين)
- جميع الحسابات المسجلة
- بيانات تسجيل الدخول
- كلمات المرور

### 2. Firestore Database (قاعدة البيانات)
- مجموعة `users` (بيانات المستخدمين)
- مجموعة `packages` (الباقات)
- مجموعة `cards` (الكروت)
- مجموعة `orders` (الطلبات)
- مجموعة `transactions` (المعاملات)
- مجموعة `vendors` (المتاجر)
- مجموعة `network_connections` (اتصالات الشبكات)
- مجموعة `sales` (المبيعات)
- مجموعة `notifications` (الإشعارات)
- مجموعة `cash_payment_requests` (طلبات الدفع النقدي)
- مجموعة `vendor_cards` (كروت المتاجر)
- مجموعة `vendor_transactions` (معاملات المتاجر)

### 3. Storage (الملفات المرفوعة)
- صور الملفات الشخصية
- أي ملفات أخرى

---

## 🔧 خطوات الحذف

### الخطوة 1: حذف حسابات المستخدمين (Authentication)

```
1. افتح Firebase Console
   https://console.firebase.google.com

2. اختر مشروعك: firebase-networkapp

3. اذهب إلى: Build → Authentication → Users

4. لكل مستخدم:
   - حدد المستخدم (✓)
   - اضغط زر "Delete account" أعلى القائمة
   - أكد الحذف

   أو للحذف الجماعي:
   - حدد جميع المستخدمين (✓ الكل)
   - اضغط "Delete account"
   - أكد الحذف
```

**مثال:**
```
المستخدمون الحاليون:
- 777854698@networkapp.app
- [أي مستخدمين آخرين]

→ حدد الكل → Delete account → تأكيد
```

---

### الخطوة 2: حذف بيانات Firestore

#### الطريقة 1: حذف يدوي (مناسب لعدد قليل من الوثائق)

```
1. في Firebase Console
2. اذهب إلى: Build → Firestore Database → Data

3. لكل مجموعة (Collection):
   
   a) users:
      - افتح المجموعة
      - لكل وثيقة، اضغط الثلاث نقاط (...)
      - اختر "Delete document"
      - أكد الحذف

   b) packages:
      - افتح المجموعة
      - احذف جميع الوثائق
   
   c) cards:
      - افتح المجموعة
      - احذف جميع الوثائق
   
   d) orders:
      - افتح المجموعة
      - احذف جميع الوثائق
   
   e) transactions:
      - افتح المجموعة
      - احذف جميع الوثائق
   
   f) vendors:
      - افتح المجموعة
      - احذف جميع الوثائق
   
   g) network_connections:
      - افتح المجموعة
      - احذف جميع الوثائق
   
   h) sales:
      - افتح المجموعة
      - احذف جميع الوثائق
   
   i) notifications:
      - افتح المجموعة
      - احذف جميع الوثائق
   
   j) cash_payment_requests:
      - افتح المجموعة
      - احذف جميع الوثائق
   
   k) vendor_cards:
      - افتح المجموعة
      - احذف جميع الوثائق
   
   l) vendor_transactions:
      - افتح المجموعة
      - احذف جميع الوثائق
```

---

#### الطريقة 2: حذف باستخدام Firebase CLI (الأسرع)

**⚠️ خطير جداً - استخدم بحذر شديد!**

```bash
# 1. تأكد من تسجيل الدخول
firebase login

# 2. تأكد من المشروع الصحيح
firebase projects:list

# 3. اختر المشروع
firebase use fir-networkapp

# 4. حذف مجموعة واحدة (مثال: users)
firebase firestore:delete users --recursive --yes

# 5. حذف جميع المجموعات (واحدة تلو الأخرى)
firebase firestore:delete users --recursive --yes
firebase firestore:delete packages --recursive --yes
firebase firestore:delete cards --recursive --yes
firebase firestore:delete orders --recursive --yes
firebase firestore:delete transactions --recursive --yes
firebase firestore:delete vendors --recursive --yes
firebase firestore:delete network_connections --recursive --yes
firebase firestore:delete sales --recursive --yes
firebase firestore:delete notifications --recursive --yes
firebase firestore:delete cash_payment_requests --recursive --yes
firebase firestore:delete vendor_cards --recursive --yes
firebase firestore:delete vendor_transactions --recursive --yes
```

---

### الخطوة 3: حذف الملفات من Storage

```
1. في Firebase Console
2. اذهب إلى: Build → Storage

3. لكل مجلد:
   - profile_images/
   - [أي مجلدات أخرى]
   
4. حدد المجلد
5. اضغط الثلاث نقاط (...)
6. اختر "Delete folder"
7. أكد الحذف
```

---

## ✅ التحقق من نظافة القاعدة

بعد الحذف، تحقق من:

### Authentication:
```
Build → Authentication → Users
✅ يجب أن تكون القائمة فارغة (0 users)
```

### Firestore:
```
Build → Firestore Database → Data
✅ يجب ألا تجد أي مجموعات أو وثائق
```

### Storage:
```
Build → Storage
✅ يجب أن يكون فارغاً أو يحتوي فقط على المجلدات الافتراضية
```

---

## 🧪 اختبار التطبيق النظيف

بعد حذف البيانات:

### 1. أعد تشغيل التطبيق

```bash
# نظف وأعد البناء
flutter clean
flutter pub get
flutter run
```

### 2. جرّب إنشاء حساب جديد

```
1. افتح التطبيق
2. اذهب للتسجيل
3. أدخل بيانات جديدة:
   - نوع الحساب: مالك شبكة
   - الهاتف: 777111222
   - كلمة المرور: Test123!
   - أكمل البيانات
4. اضغط "إنهاء التسجيل"

النتيجة المتوقعة:
✅ إنشاء الحساب بنجاح (بدون OTP)
✅ تسجيل الدخول تلقائياً
✅ الانتقال للشاشة الرئيسية
✅ التطبيق نظيف تماماً
```

### 3. تحقق من Firebase Console

```
بعد إنشاء الحساب الجديد:

Authentication → Users:
✅ يجب أن ترى مستخدم واحد فقط (777111222@networkapp.app)

Firestore → users:
✅ يجب أن ترى وثيقة واحدة فقط للمستخدم الجديد
```

---

## 📊 حالة التطبيق بعد التنظيف

### التغييرات في الكود:

1. ✅ **تم إيقاف OTP** في التسجيل
   - لا يتم إرسال رسائل SMS
   - التسجيل مباشر وسريع
   - لا حاجة لشاشة OTP

2. ✅ **App Check مُفعّل** (Debug mode)
   - للتطوير والاختبار
   - يعمل بدون مشاكل

3. ✅ **Firestore Rules محدثة**
   - تسمح بالقراءة والكتابة الآمنة
   - تم نشرها في Firebase

### البيانات:

- 🗑️ **Authentication:** نظيف (0 users)
- 🗑️ **Firestore:** نظيف (لا توجد بيانات)
- 🗑️ **Storage:** نظيف (لا توجد ملفات)

---

## ⚠️ ملاحظات مهمة

### 1. لا يمكن التراجع

```
❌ لا يوجد "Undo"
❌ لا يمكن استرجاع البيانات
✅ تأكد قبل الحذف
```

### 2. حذف البيانات ليس فورياً

```
قد يستغرق:
- Authentication: فوري
- Firestore: 1-2 دقائق
- Storage: 1-5 دقائق
```

### 3. Backup (نسخ احتياطي)

إذا أردت الاحتفاظ بنسخة احتياطية:

```
1. Export من Firestore Console
2. حفظ قائمة المستخدمين
3. تنزيل الملفات من Storage
```

---

## 🎯 ملخص سريع

```bash
# 1. حذف المستخدمين
Firebase Console → Authentication → Users → Delete all

# 2. حذف البيانات (اختر واحدة)
أ) يدوياً: Firestore Console → حذف كل مجموعة
ب) CLI: firebase firestore:delete [collection] --recursive --yes

# 3. حذف الملفات
Firebase Console → Storage → حذف المجلدات

# 4. التحقق
✅ Authentication: 0 users
✅ Firestore: فارغ
✅ Storage: فارغ

# 5. الاختبار
flutter clean && flutter pub get && flutter run
```

---

## 📞 الدعم

إذا واجهت مشاكل:
- تأكد من الصلاحيات في Firebase Console
- تحقق من أنك مسجل دخول بالحساب الصحيح
- راجع Firebase Console → Project Settings

---

**📅 التاريخ:** 2 نوفمبر 2025  
**🎯 الهدف:** تنظيف كامل للتطبيق وقاعدة البيانات  
**⚠️ تحذير:** عملية لا يمكن التراجع عنها - استخدم بحذر!

