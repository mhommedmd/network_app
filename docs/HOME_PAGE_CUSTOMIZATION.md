# دليل الصفحة الرئيسية المخصصة - Home Page Customization

## 🎯 نظرة عامة

الصفحة الرئيسية لمالك الشبكة تم تحديثها بالكامل:
- ✅ إحصائيات حقيقية من Firebase
- ✅ 3 أزرار تخصيص للباقات المفضلة
- ✅ حذف جميع البيانات الوهمية
- ✅ تخزين التخصيصات محلياً (SharedPreferences)

---

## 📱 الواجهة الجديدة

```
┌────────────────────────────────────┐
│  👤  شبكة النور للإنترنت      🔔  │
│      مرحباً، أحمد محمد             │
└────────────────────────────────────┘
│                                    │
│  ┌────────────┐  ┌────────────┐   │
│  │ المبيعات   │  │ المخزون    │   │  ⭐ حقيقية
│  │ 245,000 ر.ي│  │ 1,250 كرت  │   │
│  └────────────┘  └────────────┘   │
│                                    │
│  💰      📦      📥      🏪        │
│  دفعة    باقة    كروت    متجر    │
├────────────────────────────────────┤
│  ⭐ الباقات المفضلة               │
│                                    │
│  ┌──────────────────────────────┐ │
│  │ 📶 باقة يومية اقتصادية  ✕  │ │  ⭐ تخصيص 1
│  │    📦 150 كرت  💰 1,000 ر.ي  │ │
│  └──────────────────────────────┘ │
│                                    │
│  ┌──────────────────────────────┐ │
│  │ ➕ تخصيص 2                   │ │  ⭐ تخصيص 2
│  │    اضغط لاختيار باقة         │ │
│  └──────────────────────────────┘ │
│                                    │
│  ┌──────────────────────────────┐ │
│  │ ➕ تخصيص 3                   │ │  ⭐ تخصيص 3
│  │    اضغط لاختيار باقة         │ │
│  └──────────────────────────────┘ │
└────────────────────────────────────┘
```

---

## 🔄 الإحصائيات الحقيقية

### **1. إجمالي المبيعات:**

```dart
// حساب من transactions
Future<double> getTotalSales(String networkId) async {
  // استعلام جميع المدفوعات المكتملة
  final snapshot = await firestore
      .collection('transactions')
      .where('networkId', '==', networkId)
      .where('type', '==', 'payment')      // دفعات فقط
      .where('status', '==', 'completed')  // مكتملة فقط
      .get();
  
  // جمع المبالغ
  double total = 0;
  for (var doc in snapshot.docs) {
    total += doc.data()['amount'];
  }
  
  return total;
}
```

**ما يُحسب:**
- ✅ جميع الدفعات النقدية المكتملة
- ✅ من جميع المتاجر
- ✅ في أي وقت

### **2. المخزون المتبقي:**

```dart
// حساب من cards
await cardProvider.loadStats(networkId);
final availableCards = stats['availableCards'];
```

**ما يُحسب:**
- ✅ الكروت المتاحة فقط (status: available)
- ✅ جميع الباقات
- ✅ المجموع الكلي

---

## ⭐ نظام الباقات المفضلة

### **الفكرة:**
```
3 أزرار تخصيص → اختر باقة → تُحفظ محلياً → وصول سريع
```

### **آلية العمل:**

#### **1. أول مرة (فارغة):**
```
┌──────────────────────────────┐
│ ➕ تخصيص 1                   │  ← اضغط
│    اضغط لاختيار باقة         │
└──────────────────────────────┘
```

#### **2. عند الضغط:**
```
┌────────────────────────────┐
│        اختر باقة           │
├────────────────────────────┤
│  📶 باقة يومية (150 كرت)  │  ← اختر
│  📶 باقة أسبوعية (80 كرت) │
│  📶 باقة شهرية (50 كرت)    │
│                            │
│         [إلغاء]            │
└────────────────────────────┘
```

#### **3. بعد الاختيار:**
```
┌──────────────────────────────┐
│ 📶 باقة يومية اقتصادية  ✕  │  ← محفوظة!
│    📦 150 كرت  💰 1,000 ر.ي  │
└──────────────────────────────┘
```

### **الحفظ:**

```dart
// في SharedPreferences
SharedPreferences prefs = await SharedPreferences.getInstance();
await prefs.setString('custom_package_0_${networkId}', packageId);

// الاسترجاع
final packageId = prefs.getString('custom_package_0_${networkId}');
```

**المفاتيح:**
- `custom_package_0_{networkId}` - التخصيص الأول
- `custom_package_1_{networkId}` - التخصيص الثاني
- `custom_package_2_{networkId}` - التخصيص الثالث

---

## 🎨 التخصيصات

### **تخصيص فارغ:**
```
┌──────────────────────────────┐
│ ➕  تخصيص 1                  │
│     اضغط لاختيار باقة        │
│                          →   │
└──────────────────────────────┘
```

### **تخصيص ممتلئ:**
```
┌──────────────────────────────┐
│ 📶  باقة يومية اقتصادية  ✕  │
│     📦 150 كرت                │
│     💰 1,000 ر.ي              │
└──────────────────────────────┘
```

---

## 🔄 سير العمل

### **السيناريو 1: أول استخدام**

```
1. المستخدم يفتح الصفحة الرئيسية
   ├─ يرى إحصائيات حقيقية
   ├─ يرى 3 أزرار تخصيص فارغة
   └─ "تخصيص 1، تخصيص 2، تخصيص 3"

2. يضغط على "تخصيص 1"
   ├─ يفتح حوار الاختيار
   ├─ يرى جميع الباقات المتاحة
   └─ يختار "باقة يومية"

3. النتيجة:
   ├─ يُحفظ في SharedPreferences
   ├─ التخصيص 1 يظهر "باقة يومية"
   └─ التخصيص 2 و 3 ما زالا فارغين
```

### **السيناريو 2: تخصيص كامل**

```
المستخدم يخصص الثلاثة:
├─ تخصيص 1: باقة يومية
├─ تخصيص 2: باقة أسبوعية
└─ تخصيص 3: باقة شهرية

الآن يرى:
┌─ 📶 باقة يومية (150 كرت) ✕
├─ 📶 باقة أسبوعية (80 كرت) ✕
└─ 📶 باقة شهرية (50 كرت) ✕
```

### **السيناريو 3: إزالة تخصيص**

```
1. يضغط على ✕ في "باقة يومية"
2. يُحذف من SharedPreferences
3. يعود زر "تخصيص 1" الفارغ
```

---

## 🧪 الاختبار

### **الخطوة 1: افتح الصفحة الرئيسية**

```
تسجيل دخول كـ networkOwner
→ الصفحة الرئيسية
✅ يجب أن ترى:
   ├─ إجمالي المبيعات (حقيقي)
   ├─ المخزون المتبقي (حقيقي)
   └─ 3 أزرار تخصيص فارغة
```

### **الخطوة 2: تخصيص باقة**

```
1. اضغط "تخصيص 1"
2. اختر باقة من القائمة
3. ✅ يجب أن تظهر الباقة
4. ✅ مع المخزون والسعر
```

### **الخطوة 3: إضافة المزيد**

```
1. اضغط "تخصيص 2"
2. اختر باقة أخرى
3. اضغط "تخصيص 3"
4. اختر باقة ثالثة
5. ✅ الآن لديك 3 باقات محفوظة
```

### **الخطوة 4: إعادة فتح التطبيق**

```
1. أغلق التطبيق
2. افتحه مرة أخرى
3. ✅ التخصيصات محفوظة!
4. ✅ نفس الباقات تظهر
```

### **الخطوة 5: إزالة تخصيص**

```
1. اضغط ✕ على أي باقة
2. ✅ تختفي
3. ✅ يظهر زر "تخصيص" الفارغ
```

---

## 📊 البيانات المحفوظة

### **في SharedPreferences:**

```
للشبكة network_456:
├─ custom_package_0_network_456 = "pkg_001"
├─ custom_package_1_network_456 = "pkg_002"
└─ custom_package_2_network_456 = "pkg_003"

للشبكة network_789:
├─ custom_package_0_network_789 = "pkg_005"
├─ custom_package_1_network_789 = null
└─ custom_package_2_network_789 = "pkg_008"
```

**الفائدة:**
- ✅ كل شبكة لها تخصيصاتها الخاصة
- ✅ محفوظة محلياً
- ✅ سريعة جداً

---

## ✅ الملفات المحدثة

```
✏️ Pages:
   - network_owner_home_page.dart (تحديث شامل)

✏️ Services:
   - firebase_transaction_service.dart (getTotalSales)

✨ جديد:
   - vendor_transaction_model.dart
   - firebase_vendor_transaction_service.dart

🗑️ حذف:
   - mock_merchants.dart (استيراد)
   - Merchant class (استيراد)
   - بيانات الشبكات الوهمية
```

---

## 🎉 المقارنة

### **قبل:**
```
❌ إجمالي المبيعات: 12,450 ر.ي (وهمي)
❌ المخزون: بيانات ثابتة
❌ شبكة العاصمة (وهمية)
❌ شبكة الجنوب (وهمية)
❌ متاجر وهمية
```

### **بعد:**
```
✅ إجمالي المبيعات: من transactions (حقيقي)
✅ المخزون: من cards (حقيقي)
✅ تخصيص 1: (قابل للتخصيص)
✅ تخصيص 2: (قابل للتخصيص)
✅ تخصيص 3: (قابل للتخصيص)
✅ لا بيانات وهمية
```

---

## 💡 حالات الاستخدام

### **حالة 1: شبكة جديدة**
```
لا مبيعات بعد
├─ إجمالي المبيعات: 0 ر.ي
├─ المخزون: حسب ما تم استيراده
└─ تخصيصات فارغة
```

### **حالة 2: شبكة نشطة**
```
لديها مبيعات ومتاجر
├─ إجمالي المبيعات: 245,000 ر.ي
├─ المخزون: 1,250 كرت
└─ تخصيصات:
   ├─ باقة يومية (الأكثر مبيعاً)
   ├─ باقة أسبوعية
   └─ باقة شهرية
```

### **حالة 3: شبكة كبيرة**
```
لديها الكثير من الباقات
├─ إجمالي المبيعات: 1,500,000 ر.ي
├─ المخزون: 5,000 كرت
└─ تخصيصات:
   ├─ الباقة الأكثر طلباً
   ├─ الباقة الموسمية
   └─ الباقة VIP
```

---

## 🎯 الفوائد

### ✅ **إحصائيات دقيقة:**
- مبيعات حقيقية من Firebase
- مخزون محدث لحظياً
- لا بيانات وهمية

### ✅ **تخصيص مرن:**
- اختر باقاتك المفضلة
- وصول سريع
- 3 slots قابلة للتعديل

### ✅ **حفظ محلي:**
- SharedPreferences
- سريع جداً
- لكل شبكة تخصيصاتها

### ✅ **واجهة نظيفة:**
- لا بيانات وهمية
- كل شيء حقيقي
- احترافية عالية

---

## 🔧 الكود الأساسي

### **حساب المبيعات:**

```dart
final salesData = await FirebaseTransactionService.getTotalSales(networkId);

setState(() {
  _totalSales = salesData;
});
```

### **التخصيصات:**

```dart
// الحفظ
final prefs = await SharedPreferences.getInstance();
await prefs.setString('custom_package_0_$networkId', packageId);

// الاسترجاع
final packageId = prefs.getString('custom_package_0_$networkId');

// الحذف
await prefs.remove('custom_package_0_$networkId');
```

---

## 📱 الواجهة التفصيلية

### **البطاقة الإحصائية:**
```
┌────────────────┐
│ إجمالي المبيعات│
│ 245,000 ر.ي    │  ← حقيقي من Firebase
└────────────────┘
```

### **زر التخصيص:**
```
┌──────────────────────────────┐
│ ➕  تخصيص 1                  │
│     اضغط لاختيار باقة        │
│                          →   │
└──────────────────────────────┘
```

### **بطاقة الباقة المخصصة:**
```
┌──────────────────────────────┐
│ 📶  باقة يومية اقتصادية  ✕  │
│     📦 150 كرت                │
│     💰 1,000 ر.ي              │
└──────────────────────────────┘
```

---

## 🎉 النتيجة

### **قبل:**
```
❌ بيانات وهمية في كل مكان
❌ شبكات وهمية
❌ متاجر وهمية
❌ إحصائيات ثابتة
❌ لا تخصيص
```

### **بعد:**
```
✅ بيانات حقيقية من Firebase
✅ 3 أزرار تخصيص للباقات
✅ حفظ التخصيصات محلياً
✅ إحصائيات محدثة
✅ وصول سريع للباقات
✅ واجهة نظيفة واحترافية
```

---

**🚀 الصفحة الرئيسية الآن احترافية تماماً!** ✨

المالك يرى:
✅ مبيعاته الحقيقية  
✅ مخزونه الفعلي  
✅ باقاته المفضلة للوصول السريع  
✅ لا بيانات وهمية  

**🎊 تجربة مستخدم ممتازة!** 🎉

