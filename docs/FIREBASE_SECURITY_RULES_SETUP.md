# إعداد قواعد الأمان في Firebase

## المشكلة التي حدثت

عند محاولة إرسال دفعة نقدية، ظهر الخطأ التالي:
```
[cloud_firestore/permission-denied] 
The caller does not have permission to execute the specified operation
```

**السبب:** لم يتم تكوين Firebase Security Rules للسماح بقراءة وكتابة `cash_payment_requests`.

---

## الحل: رفع قواعد الأمان

### الخطوة 1: فتح Firebase Console

1. افتح [Firebase Console](https://console.firebase.google.com/)
2. اختر مشروعك
3. من القائمة الجانبية، اختر **Firestore Database**
4. اختر تبويب **Rules** (القواعد)

### الخطوة 2: نسخ القواعد الجديدة

افتح ملف `firestore.rules` في مجلد المشروع، وانسخ محتواه بالكامل.

### الخطوة 3: لصق القواعد في Firebase Console

1. احذف القواعد القديمة في المحرر
2. الصق القواعد الجديدة من ملف `firestore.rules`
3. اضغط **Publish** (نشر)

### الخطوة 4: التحقق

بعد النشر، سيظهر لك:
```
✅ Rules published successfully
```

---

## شرح قواعد الدفعات النقدية

### 1. القراءة (Read)
```dart
allow read: if isAuthenticated() && 
              (resource.data.vendorId == getUserId() || 
               resource.data.networkId == getUserId());
```

**الشرح:**
- ✅ مالك الشبكة يرى طلباته التي أرسلها
- ✅ المتجر يرى الطلبات المرسلة إليه
- ❌ لا يمكن للآخرين رؤية الطلبات

**مثال:**
```
الطلب:
  networkId: "network_123"
  vendorId: "vendor_456"

✅ network_123 يمكنه القراءة (هو المرسل)
✅ vendor_456 يمكنه القراءة (هو المستلم)
❌ vendor_789 لا يمكنه القراءة (ليس طرفاً)
```

### 2. الإنشاء (Create)
```dart
allow create: if isNetworkOwner() && 
                request.resource.data.networkId == getUserId();
```

**الشرح:**
- ✅ فقط مالك الشبكة يمكنه إرسال دفعات
- ✅ يجب أن يكون `networkId` في الطلب مطابق لمعرفه
- ❌ المتاجر لا يمكنهم إنشاء طلبات

**مثال:**
```
المستخدم: network_123 (networkOwner)
الطلب الجديد:
  networkId: "network_123"  ✅ مطابق
  vendorId: "vendor_456"

✅ مسموح - network_123 ينشئ طلب من حسابه
❌ ممنوع - network_123 ينشئ طلب من حساب آخر
```

### 3. التحديث (Update)
```dart
allow update: if isVendor() && 
                resource.data.vendorId == getUserId() &&
                resource.data.status == 'pending' &&
                request.resource.data.status in ['approved', 'rejected'];
```

**الشرح:**
- ✅ فقط المتجر المستلم يمكنه تحديث الطلب
- ✅ فقط إذا كانت الحالة `pending` (معلقة)
- ✅ فقط للتغيير إلى `approved` أو `rejected`
- ❌ لا يمكن تغيير أي بيانات أخرى
- ❌ لا يمكن تحديث طلب تم معالجته بالفعل

**مثال:**
```
المستخدم: vendor_456 (posVendor)
الطلب الحالي:
  vendorId: "vendor_456"
  status: "pending"

التحديث:
  status: "approved"  ✅ مسموح
  status: "rejected"  ✅ مسموح
  status: "cancelled" ❌ ممنوع (ليس في القائمة)
  amount: 10000       ❌ ممنوع (لا يمكن تغيير المبلغ)
```

### 4. الحذف (Delete)
```dart
allow delete: if isNetworkOwner() && 
                resource.data.networkId == getUserId();
```

**الشرح:**
- ✅ فقط مالك الشبكة يمكنه حذف طلباته
- ❌ المتاجر لا يمكنهم حذف الطلبات

---

## قواعد المجموعات الأخرى

### 1. **transactions** (معاملات الشبكة)
```dart
match /transactions/{transactionId} {
  allow read: if isAuthenticated() && 
                (resource.data.vendorId == getUserId() || 
                 resource.data.networkId == getUserId());
  allow create: if isAuthenticated();
}
```

**الاستخدام:** تسجيل المعاملات المالية للشبكة

### 2. **vendor_transactions** (معاملات المتجر)
```dart
match /vendor_transactions/{transactionId} {
  allow read: if isAuthenticated() && resource.data.vendorId == getUserId();
  allow create: if isAuthenticated();
}
```

**الاستخدام:** تسجيل المعاملات المالية للمتجر

### 3. **network_connections** (اتصالات الشبكات)
```dart
match /network_connections/{connectionId} {
  allow read: if isAuthenticated() && 
                (resource.data.vendorId == getUserId() || 
                 resource.data.networkId == getUserId());
  allow write: if isAuthenticated() && 
                 (resource.data.vendorId == getUserId() || 
                  resource.data.networkId == getUserId());
}
```

**الاستخدام:** تحديث رصيد المتجر مع الشبكة

---

## سيناريو كامل لعملية الدفعة النقدية

### 1️⃣ **مالك الشبكة يرسل دفعة:**
```javascript
// الطلب
POST /cash_payment_requests
{
  networkId: "network_123",      // ✅ مطابق لـ auth.uid
  networkName: "شبكة العاصمة",
  vendorId: "vendor_456",
  vendorName: "متجر الأمل",
  amount: 5000,
  note: "دفعة نقدية",
  status: "pending",
  createdAt: Timestamp
}

// القاعدة المطبقة
allow create: if isNetworkOwner() && 
                request.resource.data.networkId == getUserId();

// النتيجة: ✅ مسموح
```

### 2️⃣ **المتجر يقرأ الطلبات:**
```javascript
// الاستعلام
GET /cash_payment_requests
WHERE vendorId == "vendor_456"

// القاعدة المطبقة
allow read: if isAuthenticated() && 
              (resource.data.vendorId == getUserId() || 
               resource.data.networkId == getUserId());

// النتيجة: ✅ مسموح (vendor_456 هو المستلم)
```

### 3️⃣ **المتجر يوافق على الدفعة:**
```javascript
// التحديث
UPDATE /cash_payment_requests/req_789
{
  status: "approved",           // ✅ قيمة مسموح بها
  approvedAt: Timestamp,
  processedBy: "vendor_456"
}

// القاعدة المطبقة
allow update: if isVendor() && 
                resource.data.vendorId == getUserId() &&
                resource.data.status == 'pending' &&
                request.resource.data.status in ['approved', 'rejected'];

// النتيجة: ✅ مسموح
```

### 4️⃣ **تسجيل المعاملات تلقائياً:**

**معاملة الشبكة:**
```javascript
POST /transactions
{
  networkId: "network_123",
  vendorId: "vendor_456",
  type: "cash_payment_received",
  amount: 5000,
  // ...
}

// القاعدة: allow create: if isAuthenticated();
// النتيجة: ✅ مسموح (vendor_456 مسجل دخول)
```

**معاملة المتجر:**
```javascript
POST /vendor_transactions
{
  vendorId: "vendor_456",       // ✅ مطابق
  networkId: "network_123",
  type: "cash_payment_sent",
  amount: -5000,
  // ...
}

// القاعدة: allow create: if isAuthenticated();
// النتيجة: ✅ مسموح
```

**تحديث الرصيد:**
```javascript
UPDATE /network_connections/{connectionId}
{
  balance: 15000,  // محدث
  updatedAt: Timestamp
}

// القاعدة: allow write: if vendorId == getUserId() || networkId == getUserId()
// النتيجة: ✅ مسموح (vendor_456 طرف في الاتصال)
```

---

## الأمان والحماية

### ✅ ما تحميه هذه القواعد:

1. **منع التلاعب بالمبالغ:**
   - المتجر لا يستطيع تغيير `amount` أو أي بيانات أخرى
   - فقط `status`, `approvedAt`, `processedBy`

2. **منع الموافقة المزدوجة:**
   - التحديث فقط إذا كانت الحالة `pending`
   - بعد الموافقة/الرفض، لا يمكن التعديل مرة أخرى

3. **خصوصية البيانات:**
   - كل طرف يرى معاملاته فقط
   - لا يمكن رؤية معاملات الآخرين

4. **صلاحيات محددة:**
   - مالك الشبكة: إنشاء وحذف فقط
   - المتجر: الموافقة/الرفض فقط
   - لا أحد يستطيع التعديل العشوائي

### ❌ ما لا تسمح به:

```javascript
// ❌ متجر يحاول إنشاء طلب دفعة
POST /cash_payment_requests (from vendor)
// ممنوع: فقط mالك الشبكة

// ❌ مالك الشبكة يحاول الموافقة على طلبه
UPDATE /cash_payment_requests/{id} 
{ status: "approved" } (from networkOwner)
// ممنوع: فقط المتجر المستلم

// ❌ متجر آخر يحاول قراءة طلب ليس له
GET /cash_payment_requests/{id} 
(vendorId != currentUser)
// ممنوع: فقط الأطراف المعنية

// ❌ تغيير المبلغ بعد الإنشاء
UPDATE /cash_payment_requests/{id}
{ amount: 10000 } (from vendor)
// ممنوع: لا يمكن تغيير المبلغ
```

---

## التحقق من تطبيق القواعد

### اختبار 1: إرسال دفعة من الشبكة
```bash
# المستخدم: network_123 (networkOwner)
# العملية: إنشاء طلب دفعة

✅ يجب أن ينجح
```

### اختبار 2: قراءة الطلبات في المتجر
```bash
# المستخدم: vendor_456 (posVendor)
# العملية: قراءة الطلبات حيث vendorId == vendor_456

✅ يجب أن ينجح
```

### اختبار 3: الموافقة على الدفعة
```bash
# المستخدم: vendor_456 (posVendor)
# العملية: تحديث status من pending إلى approved

✅ يجب أن ينجح
```

### اختبار 4: تسجيل المعاملات
```bash
# المستخدم: vendor_456 (posVendor)
# العمليات:
#   1. إنشاء في transactions
#   2. إنشاء في vendor_transactions
#   3. تحديث network_connections

✅ جميعها يجب أن تنجح (في Transaction واحدة)
```

---

## خطوات النشر

### الطريقة 1: عبر Firebase Console (موصى بها)

1. **افتح Firebase Console:**
   - https://console.firebase.google.com/

2. **اذهب إلى Firestore:**
   ```
   القائمة الجانبية > Firestore Database > Rules
   ```

3. **انسخ والصق:**
   - احذف القواعد القديمة
   - الصق من ملف `firestore.rules`
   - اضغط **Publish**

4. **انتظر التحديث:**
   ```
   ⏳ Publishing... (5-10 ثوانٍ)
   ✅ Rules published successfully
   ```

### الطريقة 2: عبر Firebase CLI (للمطورين)

```bash
# 1. تثبيت Firebase CLI (إذا لم يكن مثبتاً)
npm install -g firebase-tools

# 2. تسجيل الدخول
firebase login

# 3. تهيئة المشروع (إذا لم يكن مهيأً)
firebase init firestore

# 4. نشر القواعد
firebase deploy --only firestore:rules

# النتيجة
✅ Firestore rules deployed successfully
```

---

## التحقق من نجاح النشر

### 1. من Firebase Console:
```
Firestore Database > Rules > تاريخ آخر نشر
يجب أن يظهر: "Published moments ago"
```

### 2. من التطبيق:
```dart
// جرّب إرسال دفعة نقدية
await FirebaseCashPaymentService.createPaymentRequest(request);

// ✅ يجب أن ينجح بدون خطأ permission-denied
```

---

## قواعد أساسية لجميع المجموعات

### ملخص سريع:

| المجموعة | القراءة | الكتابة | ملاحظات |
|---------|---------|---------|---------|
| `users` | الكل | نفسه فقط | بيانات المستخدمين |
| `packages` | الكل | الشبكة | الباقات |
| `cards` | الكل | الشبكة | الكروت |
| `vendor_cards` | الأطراف | الكل | كروت المتاجر |
| `orders` | الأطراف | حسب الدور | الطلبات |
| `sales` | الأطراف | المتجر | المبيعات |
| `transactions` | الأطراف | الكل | معاملات الشبكة |
| `vendor_transactions` | المتجر | الكل | معاملات المتجر |
| `network_connections` | الأطراف | الأطراف | الاتصالات |
| **`cash_payment_requests`** | **الأطراف** | **حسب القواعد** | **الدفعات النقدية** |

---

## الأخطاء الشائعة وحلولها

### خطأ 1: Permission Denied عند القراءة
```
[cloud_firestore/permission-denied] Missing or insufficient permissions
```

**الحل:**
- تأكد من نشر القواعد الجديدة
- تأكد من تسجيل دخول المستخدم
- تحقق من أن `userId` في الطلب صحيح

### خطأ 2: Permission Denied عند الكتابة
```
The caller does not have permission to execute the specified operation
```

**الحل:**
- تحقق من نوع المستخدم (`networkOwner` أو `posVendor`)
- تأكد من أن البيانات المرسلة تطابق القواعد
- راجع الحقول المطلوبة في القاعدة

### خطأ 3: Index Required
```
The query requires an index
```

**الحل:**
- استخدم الترتيب في الكود بدلاً من Firebase
- راجع ملف `NETWORK_OWNER_PAGES_IMPROVEMENTS.md` القسم 6

---

## الاختبار بعد النشر

### ✅ قائمة التحقق:

- [ ] نشر القواعد في Firebase Console
- [ ] الانتظار 10 ثوانٍ للنشر الكامل
- [ ] تسجيل خروج ودخول في التطبيق
- [ ] محاولة إرسال دفعة نقدية من حساب الشبكة
- [ ] التحقق من استلام الإشعار في حساب المتجر
- [ ] محاولة الموافقة على الدفعة
- [ ] التحقق من تسجيل المعاملات

### النتيجة المتوقعة:
```
✅ إرسال دفعة: نجح
✅ قراءة الطلبات: نجح
✅ الموافقة: نجح
✅ تسجيل المعاملات: نجح
✅ تحديث الرصيد: نجح
```

---

## نصائح مهمة

### 1. **النسخ الاحتياطي**
قبل نشر القواعد الجديدة، احتفظ بنسخة من القواعد القديمة:
```
Firebase Console > Rules > [نسخ القواعد القديمة]
```

### 2. **الاختبار في بيئة التطوير**
إذا كان لديك مشروع Firebase للتطوير، جرّب القواعد هناك أولاً.

### 3. **المراقبة**
بعد النشر، راقب:
```
Firebase Console > Firestore > Usage
```
للتأكد من عمل الاستعلامات بشكل صحيح.

### 4. **التوثيق**
احتفظ دائماً بملف `firestore.rules` في مجلد المشروع محدثاً.

---

## المساعدة والدعم

### الموارد:
- [Firebase Security Rules Documentation](https://firebase.google.com/docs/firestore/security/get-started)
- [Firebase Rules Playground](https://firebase.google.com/docs/firestore/security/test-rules-emulator)

### في حالة المشاكل:
1. تحقق من console.log في المتصفح
2. راجع Firebase Console > Firestore > Usage > Errors
3. استخدم Firebase Emulator للاختبار المحلي

---

## الخلاصة

**بعد نشر القواعد، سيعمل نظام الدفعات النقدية بالكامل:**

```
✅ إرسال الدفعات من الشبكة
✅ استلام الإشعارات في المتجر
✅ الموافقة/الرفض
✅ تسجيل المعاملات التلقائي
✅ تحديث الأرصدة
✅ أمان كامل
```

**🚀 نظام الدفعات النقدية الآن جاهز ومؤمّن!**

