# ميزة السحب للحذف (Swipe to Delete)

## نظرة عامة

تم تطبيق ميزة **Swipe to Delete** في مكانين:
1. ✅ **بطاقات المتاجر** (accounts_page)
2. ✅ **بطاقات الطلبات** (order_card) - فقط للطلبات المعالجة

---

## 1️⃣ السحب للحذف في المتاجر

### الاستخدام:

```
1. افتح: المتاجر
2. اسحب أي بطاقة متجر من اليمين لليسار 👈
3. تظهر خلفية حمراء مع "🗑️ حذف"
4. أفلت البطاقة
5. حوار تأكيد يظهر
6. اضغط "حذف" للتأكيد
```

### الكود:

```dart
Dismissible(
  key: Key(vendor.id),
  direction: DismissDirection.endToStart, // من اليمين لليسار
  confirmDismiss: (direction) async {
    if (onDelete != null) {
      onDelete!(); // يعرض حوار + يحذف
    }
    return false; // لا تحذف من UI (Stream يحدث)
  },
  background: Container(
    margin: EdgeInsets.symmetric(horizontal: 2.w, vertical: 2.h), // ✅ نفس حجم البطاقة
    alignment: Alignment.centerLeft,
    decoration: BoxDecoration(
      color: AppColors.error,
      borderRadius: BorderRadius.circular(12.r),
    ),
    child: Row([
      Icon(Icons.delete_forever),
      Text('حذف'),
    ]),
  ),
  child: AppCard(...), // بطاقة المتجر
)
```

### المميزات:
- ✅ `margin: 2.w/2.h` يجعل حجم الخلفية مطابق للبطاقة
- ✅ حوار تأكيد تلقائي
- ✅ حذف من Firebase
- ✅ Stream يحدث تلقائياً
- ✅ Toast نجاح/فشل

---

## 2️⃣ السحب للحذف في الطلبات

### الشرط:
```dart
final canDelete = order.status != 'pending' && onDelete != null;
```

**يمكن حذف الطلب فقط إذا:**
- ✅ الحالة `approved` (تمت الموافقة)
- ✅ أو الحالة `rejected` (مرفوض)
- ❌ **لا يمكن حذف** الطلبات `pending` (لازالت قيد الانتظار)

### الاستخدام:

```
1. افتح: الشبكة > الطلبات
2. ابحث عن طلب تمت معالجته (✅ موافق أو ❌ مرفوض)
3. اسحب البطاقة من اليمين لليسار 👈
4. تظهر خلفية حمراء مع "🗑️ حذف"
5. أفلت البطاقة
6. حوار تأكيد
7. اضغط "حذف"
```

### الكود:

```dart
class OrderCard extends StatelessWidget {
  const OrderCard({
    required this.order,
    this.onApprove,
    this.onReject,
    this.onDelete, // ✅ معامل جديد
    super.key,
  });

  final OrderModel order;
  final VoidCallback? onApprove;
  final VoidCallback? onReject;
  final VoidCallback? onDelete; // ✅ جديد

  @override
  Widget build(BuildContext context) {
    final canDelete = order.status != 'pending' && onDelete != null;

    if (canDelete) {
      return Dismissible(
        key: Key('order_${order.id}'),
        direction: DismissDirection.endToStart,
        confirmDismiss: (direction) async {
          // حوار التأكيد مدمج
          final confirmed = await showDialog<bool>(...);
          
          if (confirmed == true && onDelete != null) {
            onDelete!();
          }
          return false;
        },
        background: Container(
          margin: EdgeInsets.symmetric(horizontal: 2.w, vertical: 2.h), // ✅ نفس حجم البطاقة
          // خلفية حمراء
        ),
        child: _buildOrderCard(context),
      );
    }

    return _buildOrderCard(context); // طلب pending بدون Dismissible
  }

  Widget _buildOrderCard(BuildContext context) {
    return AppCard(...); // البطاقة الأصلية
  }
}
```

### التدفق:

```
الطلب pending:
  → عرض عادي
  → أزرار موافقة/رفض

الطلب approved/rejected:
  → Dismissible مفعّل ✅
  → اسحب لليمين للحذف 👈
  → حوار تأكيد
  → حذف من Firebase
```

---

## إصلاح حجم الخلفية

### المشكلة السابقة:
```
❌ حجم خلفية الحذف أكبر من البطاقة
```

### الحل:
```dart
background: Container(
  margin: EdgeInsets.symmetric(
    horizontal: 2.w,  // ✅ هامش أفقي
    vertical: 2.h,    // ✅ هامش عمودي
  ),
  // ... بقية الخصائص
)
```

**النتيجة:**
- ✅ حجم الخلفية = حجم البطاقة تماماً
- ✅ الزوايا الدائرية متطابقة (12.r)
- ✅ مظهر متناسق ومتسق

### قبل:
```
┌─────────────────────────────────────┐
│ 🗑️ حذف  [خلفية كبيرة جداً]       │ ❌
└─────────────────────────────────────┘
  ┌──────────────────────────────┐
  │ [البطاقة أصغر]              │
  └──────────────────────────────┘
```

### بعد:
```
┌──────────────────────────────────┐
│ 🗑️ حذف  [خلفية مطابقة]        │ ✅
│ [البطاقة]                       │
└──────────────────────────────────┘
```

---

## الخدمات المضافة

### 1. حذف الطلب (`firebase_order_service.dart`):

```dart
/// حذف طلب (فقط للطلبات المعالجة - approved أو rejected)
static Future<void> deleteOrder(String orderId) async {
  try {
    await _firestore.collection('orders').doc(orderId).delete();
  } catch (e) {
    throw Exception('فشل في حذف الطلب: $e');
  }
}
```

### 2. معالج الحذف (`network_page.dart`):

```dart
Future<void> _handleDelete(OrderModel order) async {
  try {
    await FirebaseOrderService.deleteOrder(order.id);
    
    if (!mounted) return;
    CustomToast.success(
      context,
      'تم حذف الطلب من السجلات',
      title: 'تم الحذف',
    );
  } catch (e) {
    if (!mounted) return;
    CustomToast.error(
      context,
      ErrorHandler.extractErrorMessage(e),
      title: 'فشل حذف الطلب',
    );
  }
}
```

---

## قواعد الأمان

### Firebase Rules المضافة:

```javascript
// Orders collection
match /orders/{orderId} {
  // ... قواعد القراءة والكتابة الحالية
  
  // ✅ الحذف: مالك الشبكة فقط
  allow delete: if isNetworkOwner() && 
                  resource.data.networkId == getUserId() &&
                  resource.data.status in ['approved', 'rejected'];
}
```

**الشروط:**
1. ✅ المستخدم يجب أن يكون `networkOwner`
2. ✅ الطلب يجب أن يكون من شبكته (`networkId == userId`)
3. ✅ الحالة يجب أن تكون `approved` أو `rejected`
4. ❌ **لا يمكن حذف** طلبات `pending`

**السبب:**
- الطلبات المعلقة قد يتم الموافقة عليها/رفضها
- بعد المعالجة، يمكن حذفها لتنظيف السجلات
- يحافظ على سجل الطلبات النشطة

---

## حوارات التأكيد

### للمتاجر:
```
╔═════════════════════════════════╗
║ ⚠️ حذف المتجر                  ║
╠═════════════════════════════════╣
║ هل تريد حذف "متجر الأمل"       ║
║ من قائمة المتاجر؟              ║
║                                 ║
║ سيتم حذف جميع البيانات المرتبطة║
╠═════════════════════════════════╣
║      [إلغاء]       [حذف]       ║
╚═════════════════════════════════╝
```

### للطلبات:
```
╔═════════════════════════════════╗
║ ⚠️ حذف الطلب                   ║
╠═════════════════════════════════╣
║ هل تريد حذف طلب "متجر الأمل"؟ ║
║                                 ║
║ سيتم حذف الطلب من السجلات.     ║
╠═════════════════════════════════╣
║      [إلغاء]       [حذف]       ║
╚═════════════════════════════════╝
```

**الفرق:**
- المتاجر: "سيتم حذف جميع البيانات المرتبطة"
- الطلبات: "سيتم حذف الطلب من السجلات"

---

## سيناريوهات الاستخدام

### سيناريو 1: حذف متجر

```
المستخدم: مالك شبكة
الصفحة: المتاجر
الإجراء:
  1. يسحب بطاقة "متجر الأمل" 👈
  2. تظهر خلفية حمراء
  3. يفلت البطاقة
  4. حوار تأكيد
  5. يضغط "حذف"
  
النتيجة:
  ✅ حذف المتجر من Firebase
  ✅ حذف جميع البيانات المرتبطة
  ✅ البطاقة تختفي من القائمة
  ✅ Toast: "تم حذف متجر الأمل"
```

### سيناريو 2: حذف طلب معالج

```
المستخدم: مالك شبكة
الصفحة: الشبكة > الطلبات
الطلب: تمت الموافقة عليه ✅
الإجراء:
  1. يسحب البطاقة 👈
  2. خلفية حمراء
  3. يفلت
  4. حوار تأكيد
  5. "حذف"
  
النتيجة:
  ✅ حذف الطلب من السجلات
  ✅ البطاقة تختفي
  ✅ Toast: "تم حذف الطلب"
```

### سيناريو 3: محاولة حذف طلب معلق

```
المستخدم: مالك شبكة
الصفحة: الشبكة > الطلبات
الطلب: قيد الانتظار ⏳
الإجراء:
  1. يحاول السحب 👈
  
النتيجة:
  ❌ لا يحدث شيء (Dismissible غير مفعّل)
  ℹ️ الطلب المعلق يحتاج موافقة/رفض أولاً
```

---

## التفاصيل التقنية

### 1. Dismissible Widget

```dart
Dismissible(
  key: Key(uniqueId),              // مفتاح فريد
  direction: DismissDirection.endToStart, // سحب من اليمين لليسار
  confirmDismiss: (direction) async {
    // عرض حوار + تنفيذ الحذف
    // return false لعدم الحذف من UI
  },
  background: Container(
    margin: EdgeInsets.symmetric(horizontal: 2.w, vertical: 2.h), // ✅ مهم!
    // خلفية حمراء
  ),
  child: Widget(), // البطاقة الأصلية
)
```

### 2. Margin للتطابق

**السبب:**
- `AppCard` لديها `margin` افتراضية
- بدون `margin` في `background`، ستكون الخلفية أكبر
- إضافة `margin: 2.w/2.h` تجعلها متطابقة تماماً

**قبل (بدون margin):**
```
█████████████████████████████ ← خلفية كبيرة
  ███████████████████████   ← بطاقة
```

**بعد (مع margin):**
```
  ███████████████████████   ← خلفية
  ███████████████████████   ← بطاقة متطابقة ✅
```

### 3. Return False في confirmDismiss

```dart
confirmDismiss: (direction) async {
  // تنفيذ الحذف
  onDelete!();
  
  return false; // ✅ مهم جداً!
}
```

**لماذا `false`؟**
- البيانات من Firebase Stream
- الحذف يحصل في Firebase
- Stream سيحدث تلقائياً
- البطاقة ستختفي عندما تحدث البيانات
- هذا يضمن تطابق UI مع البيانات

**إذا كان `true`:**
- ❌ البطاقة تختفي فوراً من UI
- ❌ قد تظهر مرة أخرى إذا فشل الحذف
- ❌ عدم تطابق بين UI والبيانات

---

## الأمان في Firebase

### قواعد المتاجر:

```javascript
match /vendors/{vendorId} {
  allow delete: if isNetworkOwner() && 
                  resource.data.networkId == getUserId();
}
```

### قواعد الطلبات:

```javascript
match /orders/{orderId} {
  allow delete: if isNetworkOwner() && 
                  resource.data.networkId == getUserId() &&
                  resource.data.status in ['approved', 'rejected']; // ✅ شرط إضافي
}
```

**الفرق:**
- المتاجر: يمكن حذف أي متجر
- الطلبات: **فقط** الطلبات المعالجة (approved/rejected)

---

## الحالات المختلفة

### حالات الطلب:

| الحالة | الأيقونة | اللون | السحب للحذف | الأزرار |
|--------|----------|-------|-------------|---------|
| `pending` | ⏳ | برتقالي | ❌ لا | موافقة/رفض |
| `approved` | ✅ | أخضر | ✅ نعم | معطلة |
| `rejected` | ❌ | أحمر | ✅ نعم | معطلة |

### رسائل Toast:

| الحالة | الرسالة | النوع |
|--------|---------|------|
| **نجاح حذف متجر** | "تم حذف متجر الأمل" | Success 🟢 |
| **نجاح حذف طلب** | "تم حذف الطلب" | Success 🟢 |
| **فشل حذف** | "فشل حذف..." | Error 🔴 |

---

## الملفات المحدثة

### الكود:
1. ✅ `lib/features/network_owner/presentation/pages/accounts_page.dart`
   - إضافة Dismissible للمتاجر
   - إصلاح حجم الخلفية

2. ✅ `lib/features/network_owner/presentation/widgets/order_card.dart`
   - إضافة معامل `onDelete`
   - إضافة Dismissible للطلبات المعالجة
   - منطق `canDelete`

3. ✅ `lib/features/network_owner/presentation/pages/network_page.dart`
   - إضافة دالة `_handleDelete`
   - تمرير `onDelete` لـ OrderCard

4. ✅ `lib/features/network_owner/data/services/firebase_order_service.dart`
   - إضافة دالة `deleteOrder`

### قواعد Firebase:
5. ✅ `firestore.rules`
   - إضافة قاعدة حذف الطلبات

### التوثيق:
6. ✅ `docs/SWIPE_TO_DELETE_FEATURE.md` (هذا الملف)

---

## الاختبار

### ✅ قائمة التحقق:

#### المتاجر:
- [ ] اسحب بطاقة متجر من اليمين لليسار
- [ ] تظهر خلفية حمراء بنفس حجم البطاقة
- [ ] حوار تأكيد يظهر عند الإفلات
- [ ] الحذف يعمل بنجاح
- [ ] البطاقة تختفي من القائمة
- [ ] Toast نجاح يظهر

#### الطلبات المعلقة (pending):
- [ ] حاول سحب طلب معلق
- [ ] لا يحدث شيء (Dismissible غير مفعّل) ✅
- [ ] الأزرار (موافقة/رفض) ظاهرة

#### الطلبات المعالجة (approved/rejected):
- [ ] اسحب طلب تمت الموافقة عليه
- [ ] تظهر خلفية حمراء بنفس حجم البطاقة
- [ ] حوار تأكيد
- [ ] الحذف يعمل
- [ ] البطاقة تختفي
- [ ] Toast نجاح

---

## المميزات

### UX:
- ✅ تجربة مألوفة (مثل Gmail, WhatsApp)
- ✅ سلسة وسهلة الاستخدام
- ✅ feedback بصري واضح (خلفية حمراء)
- ✅ حوار تأكيد آمن
- ✅ لا حذف عرضي

### الكود:
- ✅ بسيط ونظيف
- ✅ قابل لإعادة الاستخدام
- ✅ آمن (حوار تأكيد + Firebase rules)
- ✅ متزامن مع Stream

### الأمان:
- ✅ حذف محمي بـ Firebase Rules
- ✅ فقط مالك الشبكة
- ✅ فقط للطلبات المعالجة
- ✅ حوار تأكيد إلزامي

---

## نصائح الاستخدام

### 1. للمستخدم النهائي:
```
💡 نصيحة: اسحب البطاقة من اليمين لليسار للحذف
```

### 2. للمطور:
```
⚠️ تذكير: 
- انشر firestore.rules المحدث
- اختبر قواعد الحذف
- تأكد من margin في background
```

### 3. للتوسع المستقبلي:
```
يمكن إضافة Swipe to Delete لـ:
- الدفعات النقدية المعالجة
- المعاملات القديمة
- الإشعارات المقروءة
```

---

## الخلاصة

```
✅ Swipe to Delete في المتاجر
✅ Swipe to Delete في الطلبات (approved/rejected فقط)
✅ حجم خلفية مطابق للبطاقة (margin: 2.w/2.h)
✅ حوارات تأكيد آمنة
✅ قواعد Firebase محدثة
✅ 0 أخطاء
✅ تجربة مستخدم ممتازة
```

**🎉 ميزة السحب للحذف جاهزة ومتكاملة!**

